<!-- パスワードリセットメールの文面 -->
<p><%= t('devise.mailer.reset_password_instructions.greeting', name: @resource.name) %></p>

<p><%= t('devise.mailer.reset_password_instructions.body') %></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.link_text'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.notice') %></p>
