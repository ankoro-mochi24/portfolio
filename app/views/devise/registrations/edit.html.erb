<!-- URL: http://localhost:3000/users/edit -->
<!--
<h2><%= t('devise.registrations.edit.title', resource: resource_name.to_s.humanize) %></h2>

<%= form_with model: @user, url: registration_path(resource_name), local: true do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email, t('devise.registrations.edit.email') %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div><%= t('devise.registrations.edit.pending_reconfirmation', email: resource.unconfirmed_email) %></div>
  <% end %>

  <div class="field">
    <%= f.label :password, t('devise.registrations.edit.password') %> <i>(<%= t('devise.registrations.edit.leave_blank') %>)</i><br />
    <%= f.password_field :password, autocomplete: "new-password", onpaste: "return false;" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= t('devise.registrations.edit.minimum_password_length', count: @minimum_password_length) %></em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation, t('devise.registrations.edit.password_confirmation') %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", onpaste: "return false;" %>
  </div>

  <div class="field">
    <%= f.label :current_password, t('devise.registrations.edit.current_password') %> <i>(<%= t('devise.registrations.edit.current_password_instructions') %>)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password", onpaste: "return false;" %>
  </div>

  <div class="actions">
    <%= f.submit t('devise.registrations.edit.submit') %>
  </div>
<% end %>

<h3><%= t('devise.registrations.edit.cancel_account') %></h3>

<div><%= t('devise.registrations.edit.unhappy') %> <%= button_to t('devise.registrations.edit.cancel_account_button'), registration_path(resource_name), data: { confirm: t('devise.registrations.edit.cancel_account_confirmation'), turbo_confirm: t('devise.registrations.edit.cancel_account_confirmation') }, method: :delete %></div>

<%= link_to t('devise.registrations.edit.back'), :back %>
-->