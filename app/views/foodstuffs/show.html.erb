<p style="color: green"><%= notice %></p>

<%= render @foodstuff %>

<div>
  <%= link_to t('foodstuffs.edit_foodstuff'), edit_foodstuff_path(@foodstuff) %> |
  <%= link_to t('foodstuffs.back_to_list'), foodstuffs_path %>

  <% if current_user == @foodstuff.user %>
    <%= button_to t('foodstuffs.destroy_foodstuff'), @foodstuff, method: :delete %>
  <% end %>
</div>
