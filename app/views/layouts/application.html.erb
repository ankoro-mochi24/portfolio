<!DOCTYPE html>
<html>
  <head>
    <title><%= page_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- OGP メタタグ設定 -->
    <%= tag("meta", property: "og:title", content: "お米日和 - お米のレシピ＆食品投稿サイト") %>
    <%= tag("meta", property: "og:description", content: "お米を使った美味しいレシピや、お米に合う食品を投稿・共有するアプリです。ご飯の炊き方や調理手順も紹介！") %>
    <%= tag("meta", property: "og:image", content: image_url("ogp-image.jpg")) %>
    <%= tag("meta", property: "og:url", content: request.original_url) %>
    <%= tag("meta", property: "og:type", content: "website") %>
    <%= tag("meta", property: "og:site_name", content: "お米日和") %>
    <!-- Twitterカード用メタタグ -->
    <%= tag("meta", name: "twitter:card", content: "summary_large_image") %>
    <%= tag("meta", name: "twitter:title", content: "お米日和 - お米のレシピ＆食品投稿サイト") %>
    <%= tag("meta", name: "twitter:description", content: "お米のレシピや、お米に合う食品を投稿・共有！炊き方や調理工程も丁寧に解説しています。") %>
    <%= tag("meta", name: "twitter:image", content: image_url("ogp-image.jpg")) %>

    <!-- CSS のプリロード -->
    <link rel="preload" href="<%= asset_path('application.css') %>" as="style">
    <!-- JS のプリロード -->
    <link rel="preload" href="<%= asset_path('recipes.js') %>" as="script">
    <link rel="preload" href="<%= asset_path('foodstuffs.js') %>" as="script">

    <!-- スタイルシート -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css" rel="stylesheet">

    <!-- JavaScript -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="fixed-top bg-white shadow-sm">
      <%= render 'layouts/header' %>
    </header>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="container mt-5 pt-4">
      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>

    <%= javascript_include_tag 'recipes' %>
    <%= javascript_include_tag 'foodstuffs' %>
  </body>
</html>
