<h1>マイページ</h1>

<div class="main-content">
  <div class="content">
    <h2>ユーザー名</h2>
    <p><%= @user.name %></p>

    <h2>メールアドレス</h2>
    <p><%= @user.email %></p>

    <h2>持っている調理器具</h2>
    <% if @kitchen_tools.present? %>
      <ul>
        <% @kitchen_tools.each do |tool| %>
          <li><%= tool.name %></li>
        <% end %>
      </ul>
    <% else %>
      <p>調理器具はまだ登録されていません。</p>
    <% end %>

    <% if current_user.line_notify_token.present? %>
      <p>LINE Notifyと連携済み</p>
      <%= link_to "LINE Notifyとの連携を解除する", line_notify_unlink_path, data: { turbo_method: :delete }, class: "btn btn-danger" %>
    <% else %>
      <p>LINE Notifyと未連携</p>
      <%= link_to "LINE Notifyと連携する", line_notify_authorize_path, class: "btn btn-primary" %>
    <% end %>

    <%= link_to "編集", edit_profile_path, class: 'btn btn-primary mt-3' %>
  </div>
</div>
