<%= turbo_stream.replace "#{dom_id(@actionable)}_#{action_type}_actions" do %>
  <%= render partial: 'shared/user_action', locals: { actionable: @actionable, action_type: action_type } %>
<% end %>
