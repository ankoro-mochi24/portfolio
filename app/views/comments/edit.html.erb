<h1>コメントを編集</h1>

<%= form_with(model: [@commentable, @comment], local: true) do |form| %>
  <div class="field">
    <%= form.label :body, "コメント" %>
    <%= form.text_area :body %>
  </div>
  <div class="actions">
    <%= form.submit "更新する" %>
  </div>
<% end %>
